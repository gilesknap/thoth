filters:
  and:
    - file.inFolder("notes")
    - file.ext == "md"
properties:
  tags:     { displayName: Tags }
  source:   { displayName: Source }
views:
  - type: table
    name: All Notes
    order: [file.name, tags, source, updated]
    sort: [{ property: updated, direction: DESC }]
