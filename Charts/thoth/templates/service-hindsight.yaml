apiVersion: v1
kind: Service
metadata:
  name: {{ include "thoth.hindsightServiceName" . }}
  labels:
    app.kubernetes.io/component: hindsight
    {{- include "thoth.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: hindsight
    {{- include "thoth.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.hindsight.port }}
      targetPort: {{ .Values.hindsight.port }}
      protocol: TCP
